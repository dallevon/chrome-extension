import React from 'react';
import ReactDOM from 'react-dom';

const ChromeExtension: React.FC = () => <h1>Hello from Chrome Extension</h1>;

ReactDOM.render(<ChromeExtension />, document.getElementById('root'));
